---
title: Command Grammar
description: The three shapes every uxd command takes, how a bare invocation defaults, and where each verb is documented.
navigation:
  icon: i-lucide-terminal
seo:
  title: uxd Command Grammar
  description: Reference for the uxd command line — the project/ref/verb grammar, defaulted verbs, passthrough arguments, and the full verb index.
---

Every `uxd` invocation is one of three shapes.

```
uxd <project> <ref> [verb] [flags] [-- passthrough]
uxd <project> [list|clean] [flags]
uxd <top-level-verb> [args]
```

## The grammar

| Position | Meaning |
|---|---|
| `<project>` | A file `~/.uxd/<project>.toml`. `uxd projects` lists them. |
| `<ref>` | A branch, PR number, sha, path, or `-`. See [Refs](/docs/getting-started/refs). |
| `[verb]` | What to do with the workspace. Defaults to `checkout`. |
| `-- passthrough` | Everything after `--` is handed to the underlying command. |

```bash
uxd my-project 42 run test -- --watch
```

`--watch` reaches the test runner, not `uxd`.

## Defaults

Omitting the verb runs `checkout`, which materializes the workspace and prints
its path on stdout:

```bash
cd "$(uxd my-project 42)"
```

Omitting the ref runs `list`:

```bash
uxd my-project
```

::note{title="On a TTY, `uxd <project>` is a picker"}
Interactively, a bare `uxd <project>` lists the workspaces and prompts you to
pick one, then prints that workspace's path — so `cd "$(uxd my-project)"`
works. Without a TTY, or with `--json`, it falls back to a plain `list`. An
empty answer cancels.
::

## Verb index

::card-group

::card{title="Workspace verbs" icon="i-lucide-folder-git-2" to="/docs/cli/workspace-verbs"}
`checkout` · `code` · `run` · `exec` · `shell` · `sync` · `diff` · `info` · `rm`
::

::card{title="Project verbs" icon="i-lucide-boxes" to="/docs/cli/project-verbs"}
`list` · `clean`
::

::card{title="Top-level verbs" icon="i-lucide-wrench" to="/docs/cli/top-level-verbs"}
`setup` · `projects` · `doctor` · `config` · `completions` · `help` · `version`
::

::card{title="Global flags" icon="i-lucide-flag" to="/docs/cli/global-flags"}
Flags every command accepts, including `--json` and `--dry-run`.
::

::

## Built-in help

```bash
uxd help
```

prints the same grammar, the full verb list, the global flags, and the config
directory resolution order — useful when you are offline.
