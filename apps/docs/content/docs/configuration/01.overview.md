---
title: Overview
description: Where uxd looks for configuration, what lives in the config directory, and how defaults.toml relates to a project file.
navigation:
  icon: i-lucide-settings
seo:
  title: uxd Configuration Overview
  description: How uxd resolves its config directory, the layout of ~/.uxd, defaults.toml versus per-project TOML files, and how to validate them.
---

`uxd` reads per-project TOML from one config directory. There is no
project-local config file — nothing is added to your repository.

## Where the config directory is

```
--config-dir → $UXD_CONFIG_DIR → ~/.uxd
```

The first one set wins. `uxd config path` prints the winner:

```bash
uxd config path
```

```
~/.uxd
```

## Layout

```
<config-dir>/
  defaults.toml            # optional global defaults
  my-project.toml          # one file per project; filename = project name
  seeds/
    my-project/            # seed file tree for project "my-project"
      .env.local
```

Project names must match `^[a-z0-9][a-z0-9._-]*$`. `defaults` and `seeds` are
reserved.

## `defaults.toml`

Three optional keys, applied to every project that does not override them:

```toml [~/.uxd/defaults.toml]
root = "~/dev/uxd"          # base dir for derived repo_path / worktrees_path
editor = "zed"              # fallback editor preset or template
default_command = "code"    # fallback default verb
```

::warning
`env` and `commands` are **not** allowed in `defaults.toml`. Per-project config
stays self-contained on purpose — you should be able to read one file and know
what a project does.
::

## Creating and editing

```bash
uxd setup                       # scaffold the config dir and your first project
uxd config add my-project       # create a project file from a starter template
uxd config edit my-project      # open an existing one
uxd config edit                 # open defaults.toml
```

## Validating

```bash
uxd config validate
```

```
→ all project configs valid
```

Validation is **aggregate** — one run reports every schema error in a file, not
just the first. Pass a project name to check one file. Invalid config exits `3`
(`E_CONFIG`).

## Where to go next

::card-group

::card{title="Project file" icon="i-lucide-file-code" to="/docs/uxd/configuration/project-file"}
Every key a project TOML accepts, with defaults.
::

::card{title="Commands & hooks" icon="i-lucide-play" to="/docs/uxd/configuration/commands-and-hooks"}
Named commands for `run`, plus the four lifecycle hooks.
::

::card{title="Environment & templates" icon="i-lucide-braces" to="/docs/uxd/configuration/environment-and-templates"}
The `{port}` family of template variables and the `UXD_*` environment.
::

::card{title="Editors" icon="i-lucide-app-window" to="/docs/uxd/configuration/editors"}
Editor presets and custom templates.
::

::
